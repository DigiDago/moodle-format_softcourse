<div id="softcourse-introduction" class="card">
    <h1 id="softcourse-name">{{ name }}</h1>
    <div id="softcourse-summary">{{{ summary }}}</div>
    <a id="softcourse-start-button" {{#start_url}} href="{{ start_url }} " {{/start_url}}>
        <button class="btn btn-secondary" {{#disabledStart}}{{disabledStart}}{{/disabledStart}}>{{ start }}</button>
    </a>
</div>